using Chess.src.ApplicationCore.Constants;

namespace Chess.src.ApplicationCore.Input
{
    public class InputHandler
    {
        public InputID InputHandlerLoop()
        {
            // the loop condition will be true until a new module for informing gamestate is created
            while (true)
            {

            }
            return InputID.DEFAULT
        }
    }
}
